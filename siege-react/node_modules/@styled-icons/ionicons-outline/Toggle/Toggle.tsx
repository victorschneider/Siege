import * as React from 'react'
import {StyledIcon, StyledIconBase, StyledIconProps} from '@styled-icons/styled-icon'

export const Toggle: StyledIcon = React.forwardRef<SVGSVGElement, StyledIconProps>((props, ref) => {
  const attrs: React.SVGProps<SVGSVGElement> = {
      "fill": "currentColor",
  "xmlns": "http://www.w3.org/2000/svg",
  }

  return (
    <StyledIconBase
      iconAttrs={attrs}
      iconVerticalAlign="middle"
      iconViewBox="0 0 512 512"
      {...props}
      ref={ref}
    >
      <circle cx={368} cy={256} r={128} fill="none" stroke="currentColor" strokeLinejoin="round" strokeWidth={32} />

      <rect width={480} height={256} x={16} y={128} fill="none" stroke="currentColor" strokeLinejoin="round" strokeWidth={32} rx={128} ry={128} />

    </StyledIconBase>
  )
})

Toggle.displayName = 'Toggle'

export const ToggleDimensions = {height: 512, width: 512}
