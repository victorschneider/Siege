import * as React from 'react'
import {StyledIcon, StyledIconBase, StyledIconProps} from '@styled-icons/styled-icon'

export const BatteryHalf: StyledIcon = React.forwardRef<SVGSVGElement, StyledIconProps>((props, ref) => {
  const attrs: React.SVGProps<SVGSVGElement> = {
      "fill": "currentColor",
  "xmlns": "http://www.w3.org/2000/svg",
  }

  return (
    <StyledIconBase
      iconAttrs={attrs}
      iconVerticalAlign="middle"
      iconViewBox="0 0 512 512"
      {...props}
      ref={ref}
    >
      <path d="M17 384h432V128H17zm32-224h368v192H49z" />

      <rect width={185.31} height={146.16} x={70.69} y={182.92} />

      <rect width={32} height={106.67} x={465} y={202.67} />

    </StyledIconBase>
  )
})

BatteryHalf.displayName = 'BatteryHalf'

export const BatteryHalfDimensions = {height: 512, width: 512}
