import * as React from 'react'
import {StyledIcon, StyledIconBase, StyledIconProps} from '@styled-icons/styled-icon'

export const Stop: StyledIcon = React.forwardRef<SVGSVGElement, StyledIconProps>((props, ref) => {
  const attrs: React.SVGProps<SVGSVGElement> = {
      "fill": "currentColor",
  "xmlns": "http://www.w3.org/2000/svg",
  }

  return (
    <StyledIconBase
      iconAttrs={attrs}
      iconVerticalAlign="middle"
      iconViewBox="0 0 512 512"
      {...props}
      ref={ref}
    >
      <rect width={352} height={352} x={80} y={80} />

    </StyledIconBase>
  )
})

Stop.displayName = 'Stop'

export const StopDimensions = {height: 512, width: 512}
