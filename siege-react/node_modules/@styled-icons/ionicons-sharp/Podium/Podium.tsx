import * as React from 'react'
import {StyledIcon, StyledIconBase, StyledIconProps} from '@styled-icons/styled-icon'

export const Podium: StyledIcon = React.forwardRef<SVGSVGElement, StyledIconProps>((props, ref) => {
  const attrs: React.SVGProps<SVGSVGElement> = {
      "fill": "currentColor",
  "xmlns": "http://www.w3.org/2000/svg",
  }

  return (
    <StyledIconBase
      iconAttrs={attrs}
      iconVerticalAlign="middle"
      iconViewBox="0 0 512 512"
      {...props}
      ref={ref}
    >
      <rect width={192} height={448} x={160} y={32} />

      <rect width={112} height={288} x={384} y={192} />

      <rect width={112} height={352} x={16} y={128} />

    </StyledIconBase>
  )
})

Podium.displayName = 'Podium'

export const PodiumDimensions = {height: 512, width: 512}
