import * as React from 'react'
import {StyledIcon, StyledIconBase, StyledIconProps} from '@styled-icons/styled-icon'

export const Albums: StyledIcon = React.forwardRef<SVGSVGElement, StyledIconProps>((props, ref) => {
  const attrs: React.SVGProps<SVGSVGElement> = {
      "fill": "currentColor",
  "xmlns": "http://www.w3.org/2000/svg",
  }

  return (
    <StyledIconBase
      iconAttrs={attrs}
      iconVerticalAlign="middle"
      iconViewBox="0 0 512 512"
      {...props}
      ref={ref}
    >
      <rect width={256} height={32} x={128} y={64} />

      <rect width={320} height={32} x={96} y={112} />

      <path d="M464 448H48V160h416z" />

    </StyledIconBase>
  )
})

Albums.displayName = 'Albums'

export const AlbumsDimensions = {height: 512, width: 512}
