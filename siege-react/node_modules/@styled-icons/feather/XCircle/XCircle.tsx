import * as React from 'react'
import {StyledIcon, StyledIconBase, StyledIconProps} from '@styled-icons/styled-icon'

export const XCircle: StyledIcon = React.forwardRef<SVGSVGElement, StyledIconProps>((props, ref) => {
  const attrs: React.SVGProps<SVGSVGElement> = {
      "fill": "none",
  "xmlns": "http://www.w3.org/2000/svg",
  "stroke": "currentColor",
  "strokeLinecap": "round",
  "strokeLinejoin": "round",
  }

  return (
    <StyledIconBase
      iconAttrs={attrs}
      iconVerticalAlign="middle"
      iconViewBox="0 0 24 24"
      {...props}
      ref={ref}
    >
      <circle cx={12} cy={12} r={10} />

      <line x1={15} x2={9} y1={9} y2={15} />

      <line x1={9} x2={15} y1={9} y2={15} />

    </StyledIconBase>
  )
})

XCircle.displayName = 'XCircle'

export const XCircleDimensions = {height: 24, width: 24}
